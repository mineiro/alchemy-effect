import { describe, test } from "vitest";

describe("RDS Bindings", () => {
  describe("Connect", () => {
    test.skip("resolves connection settings for a bound Aurora resource", () => {});
  });
});
